import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'], variable: '--font-inter' })

export const metadata: Metadata = {
  title: 'Track | Asset, Fleet & Operational Control',
  description: 'Real-time visibility, compliance tracking, and operational control for high-value mobile assets.',
  keywords: ['asset tracking', 'fleet management', 'compliance', 'operational control', 'maintenance tracking'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="font-sans antialiased">{children}</body>
    </html>
  )
}
